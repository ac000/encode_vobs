encode_vobs
===========

A simple job scheduler to encode DVD VOB files into Ogg Theora.

It creates nr cores - 1, ffmpeg2theora processes at a time, running them at
nice +10.

Usage is like:

    $ cd /path/to/vobs
    $ encode_vobs file1.vob ...


This project is licensed under the GNU GPLv2.
